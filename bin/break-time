#!/bin/node

var electron = require('electron-prebuilt')
var proc = require('child_process')
var path = require('path')

// spawn Electron
var child = proc.spawn(electron, [path.resolve(__dirname, '../app')])